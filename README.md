## Installing
Run `npm install` to install deps

## Running the Program
Run `npm start` to run the index.js file

## Re-Running
Nodemon is used to detect the the file changes and to restart index.js

## Folder Structure
root
    data
    exclude
    output

data -> contains pages to read
exclude -> contains word to exclude
output -> contains the result